model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String?  // null если только OAuth
  name      String?
  role      Role     @default(USER)
  sessions  Session[]
  accounts  Account[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
