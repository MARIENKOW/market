
model User {
  id            String    @id @default(cuid())
  email         String   @unique
  passwordHash  String?

  status          Status @default(NOACTIVE)

  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  sessions      UserSession[]

  @@map("users")
}